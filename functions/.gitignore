# Compiled output — regenerated by `npm run build`.
lib/

# Dependencies.
node_modules/

# Local secrets for the CLI harness. Never commit real keys.
.env
.env.local

# Firebase runtime config / logs.
.firebase/
firebase-debug.log
